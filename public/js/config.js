/* ================================================================
   Настройки сайта, которые клиент может менять без пересборки.
   Файл подключается на каждой странице до основного скрипта.
   ================================================================ */
window.SITE_CONFIG = {
  // Адрес, куда отправляются заявки (POST, multipart/form-data, ответ JSON).
  // Примеры: Formspree "https://formspree.io/f/xxxxxxx", Getform, ваш скрипт на хостинге,
  // обработчик Telegram-бота. Если оставить пустым — форма не отправляет данные
  // и показывает посетителю телефоны, Telegram, MAX и e-mail.
  // ВАЖНО для 152-ФЗ: используйте сервис, который хранит данные на территории РФ.
  FORM_ENDPOINT: "",

  // Номер счётчика Яндекс.Метрики (только цифры), например "12345678".
  // Пусто — метрика не подключается. Цели: form_submit, phone_click.
  // После подключения обновите раздел «Файлы cookie» в /privacy/.
  METRIKA_ID: "",
};
const FORM_ENDPOINT = window.SITE_CONFIG.FORM_ENDPOINT;
