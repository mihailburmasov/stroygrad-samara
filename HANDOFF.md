# Хендоф: сайт на Beget, PHP-админка, почта заявок (обновлено 25 сентября 2026)

Для следующей сессии или разработчика. Общее описание проекта, команды и порядок переезда — в `README.md` ветки `php-admin`. Здесь — текущее состояние, что ждёт клиента и на что не наступать.

## Коротко: где что сейчас

- **Живой сайт — https://стройград-самара.рф** (ветка `php-admin`, Beget). Все правки — в `php-admin`, выкладка `php app/bin/deploy.php push`. Перед правкой данных или шаблонов — `deploy.php pull` (клиент мог поменять тексты и фото в админке).
- Копия на GitHub Pages (`main`) устарела: в ней нет правок 25.09 (форма «Заявка отправлена!», без кнопки «Редактировать», превью og.png, SMTP). **Решение пользователя 25.09: копию на GitHub Pages пока оставить.** Не выключать и не сливать `php-admin` в `main`, пока пользователь сам не скажет.
- Пользователь проверяет заявки сам на живом сайте (25.09): письмо на olimp@samara-7.ru должно прийти во «Входящие» от «Сайт СТРОЙГРАД» <site@стройград-самара.рф>. Если попало в «Спам» — «Не спам» и добавить отправителя в контакты.

## Сайт на хостинге (25 сентября 2026)

- Beget, аккаунт `balaba15` (там же сайт `sanitar`). Папка сайта `~/stroygrad` (`app`, `data`, `storage`, `public_html`), домен стройград-самара.рф (`xn----7sbbaani1b6anvdgcih.xn--p1ai`). PHP 8.3 и «Редирект с HTTP на HTTPS» включены в панели (⚙ у домена). SSL — Let's Encrypt.
- FTP — основной аккаунт, хост `balaba15.beget.tech`. Доступ лежит в `app/deploy.local.php`, настройки сервера — в `app/config.server.php`; оба в git не хранятся.
- Установлено через `deploy.php init`. **Дальше только `push` (код) и `pull` (правки клиента), `init --force` больше не запускать**: он перезапишет данные клиента и сменит адрес входа.
- Адрес и пароль админки выданы пользователю, в репозитории их нет.
- Заявки: Telegram-бот не задан, почта в настройках не указана → письма идут на `company.email` (olimp@samara-7.ru) через SMTP ящика сайта (см. ниже). Каждая заявка также в журнале админки.
- Грабли Beget: новый сайт получает PHP 5.6, версия меняется только в панели (`.htaccess` не помогает). FTP изредка рвёт заливку — повторный запуск докачивает, уже залитое не отправляется. На `/admin/…` без cookie Beget отдаёт JS-проверку (`beget=begetok`); браузеру не мешает, curl-проверкам — передавать этот cookie.
- Превью ссылки в мессенджерах — `og.png` 600×600 (знак как у фавикона), `og.jpg` удалён.

### Почта заявок: чтобы не попадали в спам (25.09.2026)

- Ящик сайта **site@стройград-самара.рф** (`site@xn----7sbbaani1b6anvdgcih.xn--p1ai`) создан в панели Beget → Почта. Пароль — только в конфигах сервера: `~/stroygrad/app/config.local.php` на хостинге и локальный `app/config.server.php` (в .gitignore), ключ `'smtp'`. В `storage/settings.json` его не класть: `settings_save` перезаписывает файл целиком.
- `app/lead-deliver.php` (по образцу SANITAR, коммит 0591afb): если задан `cfg('smtp')`, письмо уходит через `smtps://smtp.beget.com:465` от site@, со своими Date и Message-ID; не вышло — запасной путь `mail()` с `-f`. В ответе `smtp` = true или текст ошибки. Отправка в Telegram сохранена (сейчас бот не задан). Кнопка «Отправить проверочную заявку» в админке показывает статус SMTP.
- Проверено с сервера (с локальной машины порт 465 закрыт провайдером): временный probe → check-auth@verifier.port25.com, probe удалён. Отчёт port25: **SPF pass, iprev pass, DKIM none** (письмо не подписано). Отправитель mail1.smtp.beget.ru.
- DNS: SPF `v=spf1 redirect=beget.com`, MX — mx1/mx2.beget.com, DMARC `_dmarc` = `v=DMARC1; p=none` (добавил пользователь), DKIM `beget._domainkey` (включила поддержка Beget по тикету, 25.09).
- Повторная проверка port25 после DKIM (25.09): **SPF pass, iprev pass, DKIM pass**, подпись `d=xn----7sbbaani1b6anvdgcih.xn--p1ai; s=beget` совпадает с доменом From — DMARC выровнен.
- Мной на olimp@samara-7.ru после перехода на SMTP ничего не отправлялось — пользователь проверяет сам через форму на живом сайте. Если письма не приходят: админка → Настройки → «Отправить проверочную заявку» покажет статус SMTP и запасного пути.

## Две ветки — это главное

| Ветка | Что в ней | Где работает |
|---|---|---|
| `main` | Сайт на **Node-сборщике** (`build/*.mjs`), без админки | Старая копия: https://mihailburmasov.github.io/stroygrad-samara/ (GitHub Pages, пуш в `main` = деплой). Форма заявки там не работает. |
| `php-admin` | Сборщик переписан на **PHP** (`app/lib`) + админка (`app/admin`) + приём заявок (`app/lead.php`) + заливка по FTP (`app/bin/deploy.php`) | **https://стройград-самара.рф** — Beget, с 25.09.2026. Запушена в GitHub. |

- **Не сливать `php-admin` в `main` без решения пользователя**: формы там шлют заявки на `/lead.php`, которого на GitHub Pages нет, а workflow Pages собирает Node-сборщиком.
- До переезда (по 24.09 включительно) каждую правку делали в обеих ветках; с 25.09 правки только в `php-admin`. Так делалось всю сессию: сначала `main` (сборка, `check.mjs`, коммит, пуш, ждём деплой), потом то же в `php-admin`. Картинки переносятся через `git checkout main -- <файлы>`. JSON в `php-admin` другого формата (отступ 2, LF), поэтому данные туда вносятся через PHP (`json_pretty`), а не копированием файла из `main`.
- Переезд сделан 25.09 (`deploy.php init`). Не сделано: слить `php-admin` в `main` и выключить GitHub Pages — пользователь решил пока оставить Pages (25.09).

## Что сделано в этой сессии

**Правки живого сайта (`main`, всё выложено; то же есть в `php-admin`):**
- Главная, текст под H1 — «Объединяя таланты, создаём уникальные решения.» и «Вместе мы — сила, каждый специалист — ценность», двумя строками (перевод строки в `lead` → `<br>`). Убраны «Многопрофильная строительная компания» и «Качество · Надёжность · Опыт · Результат». В H1 убрано «в Самаре».
- Иконка Telegram в шапке увеличена до 24px.
- Коллаж hero главной: плитка с кабелем (`elec-07`) заменена фото клиента со щебёночной дорогой и погрузчиком (`asphalt-26`). Окно обрезки задано вручную в `scripts/make-art.mjs` (`extract({left:829, top:0, width:352, height:675})`), чтобы погрузчик был виден на экранах 1440 и 1920 — на них от правой полосы видна только левая часть. На телефоне эта полоса не видна вообще, так было и раньше.
- Кнопка Telegram в шапке и в мобильном меню ведёт на Telegram-канал `company.telegram`: личного Telegram у клиента нет, ссылка по номеру `t.me/+79272037373` отвечала «Пользователь не найден» (вернули на канал 2026-09-25). В карточке контактов вместо «Мессенджеры» теперь «Мы в мессенджерах» с кнопками «Telegram-канал» (`company.telegram`) и «Группа в MAX». Кнопка MAX в шапке и мобильном меню — личный чат `company.maxChat` (`https://max.ru/u/…`); в подвале и контактах — группа `company.max`.

**Ветка `php-admin`:**
- Сборщик на PHP: вывод побайтно совпадал с Node на всех 23 страницах. Порядок услуг, фоны hero, фильтры портфолио и фото главной вынесены из кода в `data/structure.json`.
- Админка: вход по секретному адресу и паролю (без пропуска — 404). Формы всех страниц, услуг, контактов и отзывов. Услуги можно добавить (создаётся скрытой), скопировать, упорядочить, удалить в корзину. Медиатека с WebP и уменьшением фото в браузере. Есть история правок, корзина, журнал заявок, настройки почты и Telegram-бота. Кнопки «Редактировать» на сайте нет (убрана 25.09 по просьбе пользователя): правки только через адрес админки.
- `deploy.php`:
  - `push` — только код, данные клиента не трогает;
  - `init <пароль>` — первая установка, сама создаёт вход в админку, SSH не нужен;
  - `pull` — забирает данные и фото с сервера; с защитой от неверного пути и от массового удаления.
- Проверено на настоящем Beget 25.09: все страницы, 404, sitemap, редиректы http→https и www, закрытые служебные папки, вход в админку, отправка письма.
- Правки 25.09 (только `php-admin`, выложено): форма после отправки прячет поля и крупно показывает «Заявка отправлена!»; убрана кнопка «Редактировать страницу»; превью ссылки — `og.png` (знак как фавикон); письма заявок через SMTP ящика site@ (SPF/DKIM pass).
- Всё ядро упаковано в глобальный навык `~/.claude/skills/php-json-admin` (для других проектов). При существенных правках ядра обновить копию в `reference/` навыка.

## Ждём от клиента

1. ~~Telegram для кнопки в шапке~~ — решено: личного Telegram у клиента нет, кнопка ведёт на канал. Если появится личный аккаунт — в `php-admin` заполнить «Telegram для сообщений» в админке (`company.telegramChat`).
2. ~~Beget~~ — сайт выложен (см. выше).
3. ~~Куда слать заявки~~ — только на почту (решение пользователя 25.09). В Telegram и MAX не отправлять.
4. **Что значит «добавить страницу»**:
   - новая услуга по шаблону — уже работает;
   - статьи или кейсы — средний объём работы;
   - произвольные страницы из блоков — большой объём.
5. Открытый вопрос пользователю: переключить нижнюю мобильную кнопку «Написать в Telegram» (сейчас ведёт в канал) на личный чат — после того как заработает п. 1.

Старые пункты из `QUESTIONS_FOR_CLIENT.md` и `CONTENT_GAPS.md` (СРО, адрес офиса, Метрика) по-прежнему открыты.

## Как работать локально (ветка `php-admin`)

```
php app/bin/build.php                                   # собрать dist/
php -S 127.0.0.1:8080 app/dev-router.php                # сайт + админка /admin/ + заявки
```
- PHP 8.3 установлен через winget: `%LOCALAPPDATA%\Microsoft\WinGet\Packages\PHP.PHP.8.3_*\php.exe`. В `php.ini` включены gd, mbstring, ftp, curl, exif, zip, intl. В PATH он попадает только после перезапуска терминала.
- `app/config.local.php` — локальные настройки, в git не хранятся (адрес 127.0.0.1:8080, без секретного адреса входа). Вход в локальную админку — пароль `test-pass-123` (хранится в `storage/`, в git не попадает, на сервер не уезжает).
- Тесты (round-trip всех разделов, API, headless Chrome, эмуляция заливки) лежат в навыке: `~/.claude/skills/php-json-admin/tests/`.

## Грабли этой сессии

- **Коллажи hero**: `npm run art` перезаписывает `data/hero.json` только переводами строк — перед коммитом `git checkout -- data/hero.json`. Добавление фото в `main` делается скриптом в папке проекта (`import sharp from 'sharp'`), а не из временной папки: там не находится `node_modules`.
- **PowerShell**: `.Replace()` с `$`, кавычками и `${...}` в коде часто молча не срабатывает — проверять результат или править через Edit. Ещё: `Remove-Item env:SITE_BASE` инструмент блокирует как «удаление корня», надо писать `$env:SITE_BASE=$null`.
- **Git Bash**: `/tmp` = `%LOCALAPPDATA%\Temp`, там лежит scratchpad сессии. Не использовать `git stash` для «проверки на чистоту»: он прячет незакоммиченную работу.
- **При смене ветки** в `main` появляются неотслеживаемые `app/` и `storage/` — это локальные файлы ветки `php-admin`. В коммиты `main` их не брать, добавлять файлы поимённо.
- **Чужие файлы**: `stroygrad-client-questions.docx` — личный документ, в git не добавлять.

---

# Предыдущая сессия: редизайн палитры и hero (сентябрь 2026)

Коммиты `7f15b9e`…`3333d68`. Относится к ветке `main` (Node-сборщик); в `php-admin` те же стили и картинки.

## Что сделано, по порядку

1. **`7f15b9e`** — цветовая схема: чёрно-золотая → синий `#1557b0` (ссылки, иконки) + оранжевый `#f97316` (только кнопки и ключевые цифры) + зелёный `--ok` (доверие, галочки). Перегенерированы favicon, og.jpg, лого (`scripts/make-brand-assets.mjs`) и коллажи hero (`scripts/make-art.mjs`).
2. **`fad9218`** — фактурный фон «под штукатурку»: `public/css/bg-plaster.css`, класс `bg-plaster` на `<body>`. Три палитры фона закомментированы в файле (активна «Цемент»).
3. **`7905bcb`** — на главной текст hero лежит прямо на фото с тёмно-синей тонировкой (`.bg-plaster .hero::after`).
4. **`097d593`** — качество hero-картинок: апскейл ×2.35, WebP q84.
5. **`a66f7b0`** — бейдж «СРО, гарантия» плавает поверх фото только при `min-height:900px`.
6. **`3e80e25`** — `bg-plaster` на всех страницах. Тон секций — позиционными селекторами `main > section:nth-of-type(odd/even)`.
7. **`d50590e`** — реквизиты (ИНН, ОГРН, название, адрес) в `data/company.json`.
8. **`ece583d`** — убраны 3 отзыва с упоминанием «Олимп».
9. **`3333d68`** — переработка hero-фото: 10 из 14 услуг на реальных фото объектов.

## Осознанные компромиссы

- `facade` используется дважды (фасады и герметизация швов). Реальных фото герметизации нет.
- `remont-pomeshcheniy` стоит на `entr-05`: фото интерьеров нет.
- `spec` (AI-спецтехника) осветлён через `BRIGHTEN`, но темнее остальных. Категория `snow` — вся `illustration`.
- `svarochnye-raboty` стоит на `weld-07`: у `weld-02` и `weld-08` водяные знаки сторонних сайтов (видно только в полном размере).
- AI-кропы `elec` и `weld` удалены из `CROPS` — не возвращать без проверки повторов.
- `.hero__sro` — `position:absolute` только при `(min-width:900px) and (min-height:900px)`, это сделано специально.

## Проблемные фото в галерее (не используются как hero)

`asphalt-19` — вывеска «ПРОДАЁТСЯ» с телефоном; `entr-06` — граффити; `weld-05` — вольер, не стройка; `weld-02`, `weld-08` — водяные знаки. В `CONTENT_GAPS.md` ещё не внесено.
